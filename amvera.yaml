meta:
  environment: nodejs
  toolchain:
    name: nodejs
    version: 20

build:
  nixPacks: false
  buildDirectory: dispatcher_web
  buildCommand: npm ci && npm run build

run:
  persistenceMount: /app/.next/cache
  runCommand: npm run start
