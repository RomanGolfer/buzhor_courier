meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  buildDirectory: .
  buildCommand: cd dispatcher_web && npm ci && npm run build
run:
  runCommand: cd dispatcher_web && npm run start
  persistenceMount: /data
