{
  "name": "buzhor-courier",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd dispatcher_web && npm ci && npm run build",
    "start": "cd dispatcher_web && node node_modules/next/dist/bin/next start -H 0.0.0.0 -p 3000"
  }
}
