{
  "name": "buzhor-courier",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd dispatcher_web && npm ci && npm run build",
    "start": "cd dispatcher_web && npm run start"
  }
}
